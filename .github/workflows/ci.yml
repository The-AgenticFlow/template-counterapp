name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Build project
        run: |
          echo "Build step - placeholder"
          echo "Update this step when the project has actual build requirements"
      
  test:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run tests
        run: |
          echo "Test step - placeholder"
          echo "Update this step when the project has actual tests"
